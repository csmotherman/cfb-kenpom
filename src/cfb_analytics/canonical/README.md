# Canonical layer

This package will convert immutable CFBD raw records into stable typed tables while preserving source meaning.

Do not add run/pass, success, explosiveness, garbage-time, or other football-derived logic here. Those belong in a future derived layer after canonical data is validated.
