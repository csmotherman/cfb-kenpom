export const metadata = {
  title: "Design template | PRIME Football",
  robots: { index: false, follow: false },
};

export default function TemplateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
